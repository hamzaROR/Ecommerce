
<%= form_for [:admin, @category] do |f| %>
  <div class="form-group">
    <%= f.text_field :name, class: "form-control", placeholder: "Enter email" %>
  </div>
  <div class="form-actions">
    <%= f.button "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
